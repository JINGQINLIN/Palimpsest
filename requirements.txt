# Python dependencies (pip install -r requirements.txt).
#
# External tools are NOT installed here and must be set up separately:
#   - Ghidra + pyghidra-mcp   (step 1: raw package fetch)
#   - CodeQL CLI              (step 5: database build)

anthropic>=0.104.1
jinja2>=3.1.6
mcp>=1.27.1
pyyaml>=6.0.3
rich>=15.0.0
